const producto = "Monitor 20\"";

//Poner en MAYUS o MINUS
console.log(producto.toUpperCase());
console.log(producto.toLocaleLowerCase()); //pon en minus antes de pasar a base de datos.

const precio = 4560

//pasar numero a string
console.log(precio);
console.log(precio.toString());

